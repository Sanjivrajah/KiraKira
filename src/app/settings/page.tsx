import { FeaturePlaceholder } from "@/components/shared/feature-placeholder";

export default function SettingsPage() {
  return <FeaturePlaceholder title="Settings" description="Additional business preferences are planned for a later session." emptyTitle="Settings workspace coming next" emptyDescription="For now, use the profile menu to sign out or reset the local demo." />;
}
