import { FeaturePlaceholder } from "@/components/shared/feature-placeholder";

export default function SettingsPage() {
  return <FeaturePlaceholder title="Settings" description="Manage your local demo workspace and business preferences." emptyTitle="No additional settings yet" emptyDescription="Use the profile menu to sign out or reset the demo. More business preferences will be added when their features are ready." />;
}
